<div class="grid_12">
<h2 id="page-heading">New Email Template</h2>

  <%= render :partial => 'form', :locals => {:farm => @farm, :email_template => @email_template} %>

<%= link_to 'Back to Email Templates', email_templates_path(:farm_id => @farm.id) %>
</div>
